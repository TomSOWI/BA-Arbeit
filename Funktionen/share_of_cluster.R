
share_of_cluster <- function(
  df,
  dfParty,
  n_cluster
)
  {
  for (i in ) {
    
  }
  nrow(dfParty[dfParty$cluster == 1,])/nrow(dfBT18[dfBT18$cluster.clara.truek.jaccard == 1,])
}


#nrow(dfClaraKLinke[dfClaraKLinke$cluster.clara.truek.jaccard == 2,])/nrow(dfBT18[dfBT18$cluster.clara.truek.jaccard == 2,])