library(usethis)
library(gh)
use_git_config(user.name = "TomSOWI", user.email = "tomklotz@gmx.net")
#gh_token(api_url = )
#gitcreds::gitcreds_get()

#use_github(protocol = "https", auth_token = Sys.getenv("GITHUB_PAt"))
#?use_github
